apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "auditvault.fullname" . }}-config
  labels:
    {{- include "auditvault.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
