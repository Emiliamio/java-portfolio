apiVersion: v1
kind: Service
metadata:
  name: {{ include "auditvault.fullname" . }}
  labels:
    {{- include "auditvault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "auditvault.selectorLabels" . | nindent 4 }}
