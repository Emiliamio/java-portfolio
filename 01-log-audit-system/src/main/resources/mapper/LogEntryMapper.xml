<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logaudit.mapper.LogEntryMapper">

    <!-- 结果映射：数据库列名 → 实体类字段 -->
    <resultMap id="logEntryMap" type="com.logaudit.entity.LogEntry">
        <id column="id" property="id"/>
        <result column="timestamp" property="timestamp"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="username" property="username"/>
        <result column="operation" property="operation"/>
        <result column="operation_result" property="operationResult"/>
        <result column="detail" property="detail"/>
        <result column="severity" property="severity"/>
        <result column="source_file" property="sourceFile"/>
        <result column="trace_id" property="traceId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 分页查询日志（多条件筛选 + 全局多字段模糊匹配） -->
    <select id="findByConditions" resultMap="logEntryMap">
        SELECT * FROM log_entry
        <where>
            <if test="startTime != null">
                AND timestamp &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND timestamp &lt;= #{endTime}
            </if>
            <if test="ipAddress != null and ipAddress != ''">
                AND ip_address = #{ipAddress}
            </if>
            <if test="operation != null and operation != ''">
                AND operation = #{operation}
            </if>
            <if test="severity != null and severity != ''">
                AND severity = #{severity}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    detail LIKE CONCAT('%', #{keyword}, '%')
                    OR username LIKE CONCAT('%', #{keyword}, '%')
                    OR ip_address LIKE CONCAT('%', #{keyword}, '%')
                    OR operation LIKE CONCAT('%', #{keyword}, '%')
                    OR source_file LIKE CONCAT('%', #{keyword}, '%')
                    OR trace_id LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
        ORDER BY timestamp DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 统计符合条件的总条数（分页用） -->
    <select id="countByConditions" resultType="long">
        SELECT COUNT(*) FROM log_entry
        <where>
            <if test="startTime != null">
                AND timestamp &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND timestamp &lt;= #{endTime}
            </if>
            <if test="ipAddress != null and ipAddress != ''">
                AND ip_address = #{ipAddress}
            </if>
            <if test="operation != null and operation != ''">
                AND operation = #{operation}
            </if>
            <if test="severity != null and severity != ''">
                AND severity = #{severity}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (
                    detail LIKE CONCAT('%', #{keyword}, '%')
                    OR username LIKE CONCAT('%', #{keyword}, '%')
                    OR ip_address LIKE CONCAT('%', #{keyword}, '%')
                    OR operation LIKE CONCAT('%', #{keyword}, '%')
                    OR source_file LIKE CONCAT('%', #{keyword}, '%')
                    OR trace_id LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
    </select>

    <!-- 根据ID查详情 -->
    <select id="findById" resultMap="logEntryMap">
        SELECT * FROM log_entry WHERE id = #{id}
    </select>

    <!-- 批量插入（异步导入用） -->
    <insert id="batchInsert" parameterType="list">
        INSERT INTO log_entry (timestamp, ip_address, username, operation, operation_result, detail, severity, source_file, trace_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.timestamp}, #{item.ipAddress}, #{item.username},
            #{item.operation}, #{item.operationResult}, #{item.detail},
            #{item.severity}, #{item.sourceFile}, #{item.traceId})
        </foreach>
    </insert>

    <!-- 今日统计（含活跃 IP 数） -->
    <select id="todayStats" resultType="map">
        SELECT
            COUNT(*) AS total,
            COALESCE(SUM(CASE WHEN severity = 'ERROR' OR severity = 'CRITICAL' THEN 1 ELSE 0 END), 0) AS abnormal,
            COUNT(DISTINCT ip_address) AS uniqueIps
        FROM log_entry
        WHERE DATE(timestamp) = CURDATE()
    </select>

    <!-- 全局统计（当今日无数据时兜底） -->
    <select id="overallStats" resultType="map">
        SELECT
            COUNT(*) AS total,
            COALESCE(SUM(CASE WHEN severity = 'ERROR' OR severity = 'CRITICAL' THEN 1 ELSE 0 END), 0) AS abnormal,
            COUNT(DISTINCT ip_address) AS uniqueIps
        FROM log_entry
    </select>

    <!-- 数据生命周期治理：物理清理截断时间点前的超期日志 -->
    <delete id="deleteBefore">
        DELETE FROM log_entry WHERE timestamp &lt; #{cutoffTime}
    </delete>
</mapper>